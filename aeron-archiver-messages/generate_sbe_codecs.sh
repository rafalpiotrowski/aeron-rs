# run this command to install sbe schema tool if not installed before
# cargo install --git https://github.com/rafalpiotrowski/sbe-schema
sbe schema generate --file schema/messages.xml --language rust

